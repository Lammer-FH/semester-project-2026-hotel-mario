# Database
DB_URL=jdbc:mysql://localhost:3306/hotelmario_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
DB_USERNAME=hotelmario
DB_PASSWORD=hotelmario_password

# JPA — use "validate" in production (requires Flyway or similar)
DB_DDL_AUTO=update

# API context path (default: /api)
API_PREFIX=/api
